const load = (/* parseGraphQLSchema, parseClass */) => {
  // const findGraphQLQueryName = `find${parseClass.className}`;
  // parseGraphQLSchema.graphQLQueries[findGraphQLQueryName] = {
  // };
};

export { load };
